"use strict";

function GearboxFilter(rest) {
    var onChange = function onChange(event) {
        console.log("onChange.target", event.target.value);
    };
    var filterJson = rest.json();
    return function (props) {
        var options = rest.domain().makeOptions(filterJson.selected, rest.json().legalValues);
        return React.createElement(
            "div",
            { key: filterJson.filterName },
            "Gearbox Filter",
            React.createElement(
                "select",
                { onChange: function (event) {
                        return onChange(event);
                    } },
                options
            )
        );
    };
}
var e = {
    "_render": { "_self": "#GearboxFilter/render#" },
    "filterName": "filter.transmission",
    "selected": null,
    "legalValues": ["Automatic", "Gearbox"]
};
//# sourceMappingURL=GearboxFilter.js.map
return GearboxFilter
