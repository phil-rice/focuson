'use strict';

function Cpq(rest) {
    var filters = function filters(json) {
        return json.filters.map(function (f, i) {
            return React.createElement(Rest, { rest: rest.then('filters').withLens(Lens.nth(i)) });
        });
    };

    return function (props) {
        return React.createElement(
            'div',
            { className: 'cpq' },
            filters(rest.json())
        );
    };
}
//# sourceMappingURL=Cpq.js.map
return Cpq
