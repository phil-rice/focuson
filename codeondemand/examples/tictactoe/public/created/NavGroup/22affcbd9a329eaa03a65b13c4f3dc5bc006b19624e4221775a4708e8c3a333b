'use strict';

function NavGroup(props) {
    var name = props.context.json().name;
    var group = props.context.json().jsonFiles.map(function (url, i) {
        return(
            // @ts-ignore //TODO consider how to remove this... how do we check that the name is legit? Or do we care? because we have a run time check with message anyway...
            React.createElement(
                'li',
                { key: url },
                React.createElement(
                    'a',
                    { onClick: function () {
                            return props.context.domain.loadUrlAndPutInElement(name, url, 'root');
                        } },
                    url
                )
            )
        );
    });
    return React.createElement(
        React.Fragment,
        null,
        React.createElement(
            'li',
            { key: 'groupName' },
            props.context.json().name
        ),
        React.createElement(
            'ul',
            { key: 'navGroup' + name },
            group
        )
    );
}
//# sourceMappingURL=NavGroup.js.map
return NavGroup
