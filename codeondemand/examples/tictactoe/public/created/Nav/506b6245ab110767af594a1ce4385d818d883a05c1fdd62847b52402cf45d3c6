'use strict';

function Nav(rest) {
    return function (props) {
        console.log("Nav.loadUrlAndPutInElement", rest.domain().loadUrlAndPutInElement);
        var groups = rest.json().groups.map(function (g, i) {
            return React.createElement(Rest, { key: i, rest: rest.then('groups').withLens(Lens.nth(i)) });
        });
        return React.createElement(
            'ul',
            { key: 'groups' },
            groups
        );
    };
}
//# sourceMappingURL=Nav.js.map
return Nav
