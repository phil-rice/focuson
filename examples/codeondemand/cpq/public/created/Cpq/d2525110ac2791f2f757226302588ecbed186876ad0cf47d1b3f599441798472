'use strict';

function Cpq(props) {
    var filters = props.context.json().filters.map(function (f, i) {
        return React.createElement(ComponentFromServer, { key: i, context: props.context.focusOn('filters').withChildLens(Lens.nth(i)) });
    });
    var price = props.context.json().price.toString();
    return React.createElement(
        'div',
        { className: 'cpq' },
        React.createElement(
            'div',
            { key: 'summary', className: 'summary' },
            'Price: ',
            price
        ),
        React.createElement(
            'div',
            { key: 'filters', className: 'filters' },
            filters
        )
    );
}
//# sourceMappingURL=Cpq.js.map
return Cpq
