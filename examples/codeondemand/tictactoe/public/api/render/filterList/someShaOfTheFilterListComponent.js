//Here we put the filterList Component that will take the file and render it to the screenF

//it's a react java script component



function FilterList(props) {
    (<button className='square' onClick={() => props.onClick() }>{props.value + "x"}</button>)
}

FilterList